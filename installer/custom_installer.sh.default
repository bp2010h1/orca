#!/bin/bash

# Put your own values here
USER="aUser"
PW="aPw"
VM="/aPath/to/vm"

CONFIG="ConfigurationOfOrca" 
# test image 	=>  ConfigurationOfOrca				(only Orca packages and their dependencies)
# dev image 	=>	ConfigurationOfBP2010H1		(Orca packages with dependencies, Preferences, OmniBrowser, Refactoring for OB, OCompletion, OCforOB, Shout)

./installer.sh -u "$USER" -p "$PW" -v "$VM" -c "$CONFIG"